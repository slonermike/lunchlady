export default {
    /**
     * Output a string to our specified output channel.
     *
     * @param str String to output.
     */
    log: function(str: string) {
        console.log(str);
    }
}

